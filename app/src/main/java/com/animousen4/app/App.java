/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.animousen4.app;

import com.animousen4.list.LinkedList;

import static com.animousen4.utilities.StringUtils.join;
import static com.animousen4.utilities.StringUtils.split;
import static com.animousen4.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
