spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

game:
  engine:
    stockfish: /engine/stockfish16

time:
  zoneId: Europe/Minsk

user:
  root:
    username: root
    password: 123456

jwt:
  secret:
    public: jwt-keys/rsaPub.pub
    private: jwt-keys/rsaPriv.priv
  lifetime: 60m